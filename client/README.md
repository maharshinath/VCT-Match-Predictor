# Client (React + Vite)

Frontend for **VCT Match Predictor**. See the root [README](../README.md) for full setup, data pipeline, and API docs.

```bash
npm install
npm run dev
```

App: http://localhost:5173 — expects the Flask API at http://127.0.0.1:5001.
